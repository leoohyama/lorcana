.env
/.quarto/
**/*.quarto_ipynb

# macOS Finder metadata (churns constantly; a cross-machine conflict source)
.DS_Store
**/.DS_Store

# Local Claude Code tooling (never share)
.claude/
CLAUDE.md
CLAUDE.local.md
__pycache__/

# root build artifact only (leading slash); the committed copy lives in docs/
/experimental_cards.json
